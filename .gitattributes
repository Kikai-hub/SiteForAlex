# Force LF regardless of core.autocrlf — these run as shell scripts on Linux
# servers, and a stray CRLF breaks the shebang line there in ways that are
# maddening to diagnose from a Windows dev machine.
*.sh text eol=lf
bin/* text eol=lf
Dockerfile text eol=lf
*.env* text eol=lf
